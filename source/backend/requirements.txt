# ============================================================
#  Backend dependencies — Situs Personal Feri + AI Assistant
#  Python 3.11+   |   install: pip install -r requirements.txt
# ============================================================

# --- Web framework & server ---
fastapi>=0.115
uvicorn[standard]>=0.32

# --- Agent framework + LLM (OpenRouter via OpenAI-compatible client) ---
agno>=1.1.0
openai>=1.54            # dipakai untuk memanggil OpenRouter (base_url override)

# --- Config & validation ---
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0

# --- Persistensi (chat history) ---
# sqlite3 sudah bawaan Python (dev). libsql-client untuk Turso (prod).
libsql-client>=0.3.1

# --- HTTP async ---
httpx>=0.27

# --- Pipeline konversi knowledge base (HTML/PDF -> markdown) ---
beautifulsoup4>=4.12
markdownify>=0.13
pymupdf4llm>=0.0.17

# --- Rate limiting ---
slowapi>=0.1.9
